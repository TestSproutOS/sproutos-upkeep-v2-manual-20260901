# SproutOS upstream upkeep acceptance

Disposable upstream repository used to verify snapshot-copy, literal-fork, and manual-upstream reconciliation in production.

Initial marker: `v1`.

Upstream update marker: `v2`, published after all three SproutOS project strategies were recorded.
